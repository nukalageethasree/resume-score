# Core environment
pydantic>=2.0,<3.0
pyyaml>=6.0

# Web server (replaces Gradio — no version conflicts)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.9

# PDF extraction (optional but recommended)
PyMuPDF>=1.23.0

# Baseline inference
openai>=1.0,<2.0

# Testing
pytest>=7.0
